import { createSlice } from "@reduxjs/toolkit";
const initialState = {};
const gateSlice = createSlice({
  name: "gate",
  initialState,
  reducers: {},
  extraReducers: {},
});

export default gateSlice.reducer;
